{
  "scripts": {
    "config": "cp -f config.js docs/.vuepress",
    "dev": "npm run config & vuepress dev docs",
    "start": "npm run dev",
    "build": "npm run config & vuepress build docs",
    "deploy": "sh deploy.sh"
  },
  "devDependencies": {
    "vuepress": "^1.9.7"
  }
}
